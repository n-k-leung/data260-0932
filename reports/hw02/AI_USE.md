1. I used AI assistent to help debug errors with how Pydantic schema worked and how to do the return on failure part. I was able to get the Pytdantic schema but did not know how to reference it. In the planner_node function it helped me reference how to include in the promp the retry error and do the try block. I orginally had just the loading from json output from the llm which follows what I did for part 3 in the homework.
2. In the reviewer_node it gave me to put the has_changes to be feedback["has_changes"]={
        feedback.get("tags") != state["planner_proposal"].get("tags") or feedback.get("summary") != state["planner_proposal"].get("summary")
    }. 
This is not the correct format becuase has changes is boolean and this makes it a list so the data type was wrong and there was error. I was able to see this when debugging in the output console. ThereforeI changed the curly brackets to paraenthesis instead to make it boolean and work.
3. I detected a problem with the above problem by checking the output console and noticing and type error that was evoked. I was able to verify the correct result after the change to the parenthesis by rerunning the part4.py and looking at the output to see any type errors regarding this boolean. 
4. Another thing I changed was adding a wait time before loading the tables to get the loading state. This line allowed my computer to see the popup message that it was loading the tables to verify this state was correctly being called upon. Before, I followed the advice to just call the loading state in the to try and catch the error. However, my computer runs quickly so the loading state never showed. Therefore, I added a line to make it work to set a timeout and add a delay to see it.