1. I used AI to help with how to print out the recall@k. I had created the rest of the metrics table but needed help with the logic behind the recall@k column. I asked it to give the setence logic for how to set it up so I implemented that logic as a function. This recall function calculates the recall or true correct outputs/(true correct outputs+ true false outputs). I also used AI to help me understand the python packages more indepth so I can figure out the questions to put in the questions.yaml package. I then created the questions so it also satisfies the requirement of having two questions with one package source as the answer.
2. One AI output that was wrong was that when writing to a file there was a unicode error when trying to write each package to their own txt. It mentioned that codec can't encode a character within the data. I consulted AI to help output each python package json into a txt file. While the version split function had no error, I found the error with encoding.
3. I was able to detect the error mentioned previously by when running the get_packages.py file with the command python get_packages.py. It gave the error UnicodeEncodeError where it mentioned an encoding issue.
4. I changed the file where I write to each python file as a txt file to include the encoding paramter. I set the encoding to "utf-8", which is the most typical encoding to solve the issue. I then reran the command python get_packages.py. As a result, the python file run and I was able to verify that the txt for each python open source package with vulnerabilites had a txt file that was not empty. I also verified in the console as I put a print statement to print each txt and the number of vulnerabilites per package.txt to verify each package.txt was populated.